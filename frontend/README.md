# planlog frontend

Next.js(App Router) 기반 Daily Timeboxing Planner 화면. 규칙은 `CLAUDE.md` 참고.

## 실행

```bash
npm install
npm run dev
```

## 빌드/검증

```bash
npm run build
npx eslint .
npx tsc --noEmit
```
